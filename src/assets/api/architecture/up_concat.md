

### up_concat
```python
up_concat(conv_pooled, conv, factor, nchannels, window, strides=(1, 1, 1), dropout=None, bn=None, activation=None, idx=0, bias=False, dilation_rate=1, upsampling='copy', residual=False)
```
